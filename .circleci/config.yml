version: 2.1
 
jobs:
  config_cisco:
    machine: true
    resource_class: harutoendo/cisco-resouce
    steps:
      - run:
          name: Runner test
          command: |
            echo "CircleCI Runner OK"
            whoami
            hostname
            pwd
 
workflows:
  config:
    jobs:
      - config_cisco